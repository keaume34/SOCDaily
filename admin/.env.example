# Optional password gate (set to enable login wall).
ADMIN_PASSWORD=
# Path to the seed bank consumed by both the app and the admin web.
# Defaults to ../app/assets/seed relative to admin/.
SOCDAILY_SEED_DIR=
